"""Interactive Rich-based UI.

Modules:
- ``widgets`` — small reusable helpers (admin badge, locale meta, console).
- ``wizard``  — first-run language / country pickers.
- ``menu``    — main numbered menu rendering + feature intro panel.
- ``actions`` — dispatch table mapping menu actions to real work.
"""
