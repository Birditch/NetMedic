# .omc

This directory is intentionally visible to the repository so contributors
can add shared OpenAI/Codex project conventions when needed.

Do not commit local runtime state, session logs, temporary task output, or
nested Git checkouts here. Generated GitHub Wiki pages are published to the
separate `Birditch/NetMedic.wiki.git` repository.
